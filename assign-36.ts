// T - Shirt: Write a function called make_shirt() that accepts a size and the text of a message that
// should be printed on the shirt.

function make_shirt(size: number, message: string) {
    // The function should print a sentence summarizing the size of the shirt and the message printed on it.
    console.log(`The size of the shirt is "${size}" and the message print on the shirt is "${message}"`);
    
}
//  Call the function.
make_shirt(34,"Muhammad Talha")